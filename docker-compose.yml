services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./app:/usr/share/nginx/html
    environment:
      - NODE_ENV=development
    restart: unless-stopped

networks:
  default:
    name: lumina-network
